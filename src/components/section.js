import React from "react"

import  "./css/section.module.css"

const Section = ({ children }) => {
    return (
        <>
            <div>{children}</div>
        </>
    )
}

export default Section