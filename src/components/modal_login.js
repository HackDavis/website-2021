import React, { useState } from 'react';
import Modal from "./modal"
import styles from "./css/modal_login.module.css"

const LoginModal = ( {children}) => {

    return 
        <Modal onClick={() => this.setIsModalOpen(true)}></Modal>
    ;
}

export default Modal